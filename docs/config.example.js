// Copy this file to config.js and fill in your values.
// NEVER commit docs/config.js — it is listed in .gitignore.
//
// How to get these values:
// 1. Go to https://console.cloud.google.com/
// 2. Create a project (or select an existing one).
// 3. Enable the "Google Drive API".
// 4. Go to "APIs & Services" → "Credentials" → "Create Credentials" → "OAuth client ID".
// 5. Application type: "Web application".
// 6. Add Authorized JavaScript origins:
//      http://localhost:8080   (or your local dev port)
//      https://<your-github-username>.github.io
// 7. Copy the generated Client ID below.
//
// Security note: During development, limit to "Test users" in the OAuth consent screen.
// Do NOT publish unrestricted credentials.

window.APP_CONFIG = {
  // Your Google OAuth 2.0 Client ID
  googleClientId: "YOUR_CLIENT_ID_HERE.apps.googleusercontent.com",
};
